using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class Enums
{
    public enum HeroClass { paladin, theif, bard }
    public enum MonsterType { skeleton, mushroom, flyingEye, goblin }
    public enum potionType { health, stamina }
    public enum ItemType { potion, weapon, armor }
}
